[project]
name = "optimistic-composite-sanitizer"
authors = ["web3-dev-community"]
description = "An innovative smart contract for optimized validation and compositional data management"
telemetry = false
requirements = []
[contracts.optimistic-composite-sanitizer]
path = "contracts/optimistic-composite-sanitizer.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
